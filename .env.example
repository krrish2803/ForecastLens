# AIgnition Forecast — Environment Configuration

# API server
HOST=0.0.0.0
PORT=8000

# Paths (relative to project root)
DATA_DIR=./data
OUTPUT_DIR=./output
MODEL_PATH=./pickle/model.pkl
FEATURES_PATH=./features.parquet

# Data quality gating sensitivity (1.0 = neutral, >1 = more conservative)
GATING_ALPHA=1.5

# LLM summary (optional — leave empty for fallback)
ANTHROPIC_API_KEY=
